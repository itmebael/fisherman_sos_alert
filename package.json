{
  "name": "fisherman-sos-alert",
  "version": "1.0.0",
  "description": "BantayDagat - Mobile Rescue And Location Tracking System - Static Site (Pre-built)",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed - using pre-built Flutter web files from build/web'",
    "vercel-build": "echo 'No build needed - using pre-built Flutter web files from build/web'"
  }
}
