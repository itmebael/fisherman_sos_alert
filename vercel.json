{
  "buildCommand": "test -d build/web && echo 'Using pre-built Flutter web files from build/web' || (echo 'Error: build/web directory not found' && exit 1)",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
