{
  "buildCommand": "bash vercel-build.sh || (echo 'Build script failed, checking for pre-built files...' && test -d build/web && echo 'Pre-built files found' || exit 1)",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
